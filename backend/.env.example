# Server
PORT=4000
CLIENT_URL=http://localhost:5173

# Google service account credentials
GOOGLE_SERVICE_ACCOUNT_JSON='{"type": "...", "project_id": "..."}'

# Comma-separated Google Sheet IDs
SHEET_IDS=sheetId1,sheetId2

# Admin credentials
ADMIN_USER=adminuser
ADMIN_PASS=supersecretpassword

# JWT secrets
JWT_SECRET=replace_with_long_random_secret_for_session
VIDEO_TOKEN_SECRET=replace_with_another_long_secret

# Token lifetimes
JWT_EXPIRES=8h
VIDEO_TOKEN_EXPIRES=7d
